var preco = Number(prompt("dfigite o preço do produto!"))
var vista = (preco * 10) / 100
var resultado = preco - vista 
var parcela = (preco / 3)
alert(" o valor do produto é: " + preco + 
"\n a vista vai ser: " + resultado + (
    "n\ os 3x:" + parcela
) )
    
